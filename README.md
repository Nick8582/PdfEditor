# PDF Editor - Редактор PDF

Графическое приложение для редактирования PDF файлов с возможностью удаления областей и замены цветов.

## 🆕 Веб-версия для iPad

Теперь доступна веб-версия приложения, которая работает на iPad и других устройствах через браузер!

## Возможности

- 📄 Загрузка и просмотр PDF файлов
- 🗑️ Выделение и удаление областей на страницах
- 🎨 Замена цветов в выделенных областях
- 🔍 Масштабирование страниц
- 📑 Навигация по страницам
- 💾 Сохранение отредактированных PDF файлов

## Требования

- Python 3.7 или выше
- Tkinter (обычно входит в стандартную установку Python)
- PyMuPDF (fitz)
- Pillow (PIL)

## Установка

### 1. Клонирование или загрузка проекта

Если проект еще не скачан, скачайте файл `main.py`.

### 2. Установка зависимостей

Установите необходимые библиотеки с помощью pip. Если команда `pip` не найдена, попробуйте `pip3` или `python3 -m pip`:

**Вариант 1: Прямая установка**

```bash
pip install PyMuPDF Pillow
```

Если `pip` не найден, попробуйте:

```bash
pip3 install PyMuPDF Pillow
```

Или:

```bash
python3 -m pip install PyMuPDF Pillow
```

**Вариант 2: Использование requirements.txt**

```bash
pip install -r requirements.txt
```

Если `pip` не найден, попробуйте:

```bash
pip3 install -r requirements.txt
```

Или:

```bash
python3 -m pip install -r requirements.txt
```

**Проверка установки pip:**

```bash
# Проверьте, какая команда доступна
which pip
which pip3
python3 --version
python3 -m pip --version
```

### 3. Проверка установки Tkinter

Tkinter обычно входит в стандартную установку Python. Если его нет, установите:

**Ubuntu/Debian:**

```bash
sudo apt-get install python3-tk
```

**macOS:**
Tkinter обычно уже установлен. Если нет, установите через Homebrew:

```bash
brew install python-tk
```

**Windows:**
Tkinter обычно входит в установку Python. Если нет, переустановите Python с официального сайта.

## Запуск

Запустите приложение командой:

```bash
python main.py
```

Или для Python 3:

```bash
python3 main.py
```

**Примечание:** На macOS и некоторых Linux системах может потребоваться использовать `python3` вместо `python`.

## Использование

### Загрузка PDF

1. Нажмите кнопку **"Загрузить PDF"**
2. Выберите PDF файл в диалоговом окне
3. Файл откроется для редактирования

### Выделение областей для удаления

1. Нажмите кнопку **"Выделить для удаления"**
2. Нажмите и удерживайте левую кнопку мыши на странице
3. Перетащите мышь, чтобы выделить область
4. Отпустите кнопку мыши
5. Выделенная область будет отмечена красным пунктиром

### Выделение областей для замены цвета

1. Нажмите кнопку **"Выделить для замены цвета"**
2. Выберите целевой цвет, нажав на цветную кнопку рядом с "Цвет замены:"
3. Нажмите и удерживайте левую кнопку мыши на странице
4. Перетащите мышь, чтобы выделить область
5. Отпустите кнопку мыши
6. Выделенная область будет отмечена синим пунктиром

### Удаление элементов

**Способ 1: Клик по области**

1. Нажмите кнопку **"Удалить элемент"**
2. Кликните по любой выделенной области на странице
3. Область будет удалена

**Способ 2: Из списка**

1. Выберите элемент в списке "Области для удаления" или "Замены цвета"
2. Нажмите кнопку **"Удалить выбранное"** или дважды кликните по элементу
3. Элемент будет удален

### Навигация

- Используйте кнопки **←** и **→** для перехода между страницами
- Используйте кнопки **+** и **-** для масштабирования
- Кнопка **"Сброс"** возвращает масштаб к 100%
- Можно использовать колесико мыши для масштабирования

### Сохранение

1. Нажмите кнопку **"Сохранить PDF"**
2. Выберите место для сохранения и введите имя файла
3. Нажмите "Сохранить"

### Очистка выделений

Нажмите кнопку **"Очистить выделения"** для удаления всех выделений на текущей странице.

## Структура проекта

```
pdf-editor/
├── main.py              # Основной файл приложения
├── requirements.txt     # Список зависимостей
└── README.md           # Инструкция по использованию
```

## Решение проблем

### Ошибка "command not found: pip"

На macOS и некоторых Linux системах команда `pip` может быть недоступна. Используйте альтернативные варианты:

```bash
# Вариант 1: Используйте pip3
pip3 install -r requirements.txt

# Вариант 2: Используйте python3 -m pip
python3 -m pip install -r requirements.txt

# Вариант 3: Установите pip через ensurepip (если Python установлен, но pip отсутствует)
python3 -m ensurepip --upgrade
```

**Проверка доступных команд:**

```bash
which python3
which pip3
python3 --version
```

### Ошибка "No module named 'tkinter'"

Установите Tkinter для вашей системы (см. раздел "Проверка установки Tkinter").

### Ошибка "No module named 'fitz'"

Установите PyMuPDF:

```bash
pip3 install PyMuPDF
# или
python3 -m pip install PyMuPDF
```

### Ошибка "No module named 'PIL'"

Установите Pillow:

```bash
pip3 install Pillow
# или
python3 -m pip install Pillow
```

### Приложение не запускается

Убедитесь, что:

- Python версии 3.7 или выше установлен
- Все зависимости установлены
- Вы запускаете скрипт из правильной директории

## 🌐 Веб-версия для iPad и других устройств

### Запуск веб-сервера

1. Установите дополнительные зависимости:

```bash
pip install Flask flask-cors
```

2. Запустите веб-сервер:

```bash
python app.py
```

3. Откройте браузер и перейдите по адресу:

   - На компьютере: `http://localhost:5001`
   - На iPad (в той же сети Wi-Fi): `http://[IP_адрес_компьютера]:5001`

   **Примечание**: Порт 5001 используется вместо 5000, так как на macOS порт 5000 часто занят AirPlay Receiver.

   Чтобы узнать IP адрес:

   - macOS/Linux: `ifconfig | grep "inet "`
   - Windows: `ipconfig`

### Использование на iPad

1. Убедитесь, что iPad подключен к той же Wi-Fi сети, что и компьютер
2. Откройте Safari на iPad
3. Введите адрес `http://[IP_адрес]:5001`
4. Загрузите PDF файл и начните редактирование

### Особенности веб-версии

- ✅ Работает на iPad, iPhone и других устройствах
- ✅ Поддержка сенсорных жестов
- ✅ Адаптивный интерфейс
- ✅ Все функции десктопной версии
- ✅ Сохранение PDF файлов

### Безопасность

⚠️ **Важно**: Веб-сервер запускается на всех интерфейсах (0.0.0.0), что означает доступ из локальной сети. Используйте только в доверенной сети Wi-Fi.

Для продакшн-использования рекомендуется:

- Настроить HTTPS
- Добавить аутентификацию
- Использовать reverse proxy (nginx)

## 📦 Сборка exe файла для Windows

Вы можете собрать исполняемый exe файл для Windows, чтобы запускать приложение без установки Python на других компьютерах.

### Требования для сборки на Windows

1. **Python 3.7 или выше** - скачайте с [python.org](https://www.python.org/downloads/)
   - ⚠️ При установке обязательно отметьте "Add Python to PATH"
2. **Все зависимости** из `requirements.txt`

### Пошаговая инструкция для Windows

#### Шаг 1: Установка Python

1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. Запустите установщик
3. **Важно:** Отметьте галочку "Add Python to PATH"
4. Нажмите "Install Now"

#### Шаг 2: Установка зависимостей

Откройте командную строку (cmd) или PowerShell в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

Если `pip` не найден, попробуйте:

```bash
python -m pip install -r requirements.txt
```

Или установите PyInstaller отдельно:

```bash
pip install pyinstaller
```

#### Шаг 3: Сборка exe файла

**Способ 1 (проще всего):**
Просто дважды кликните на файл `build_exe.bat`

**Способ 2 (через командную строку):**

```bash
build_exe.bat
```

**Способ 3 (вручную):**

```bash
python build_exe.py
```

#### Шаг 4: Готово!

После сборки exe файл будет находиться в папке `dist/PDFEditor.exe`

Вы можете:

- Скопировать `PDFEditor.exe` на любой компьютер с Windows
- Запустить его двойным кликом - Python не требуется!

### Дополнительная информация

**Время сборки:** Обычно занимает 2-5 минут в зависимости от скорости компьютера

**Размер exe файла:** Примерно 50-100 МБ (включает все зависимости)

**Распространение:**

- Скопируйте `dist/PDFEditor.exe` на любой компьютер с Windows
- Python и другие зависимости НЕ требуются на целевом компьютере
- Просто запустите exe файл двойным кликом

### Сборка веб-приложения (app.py)

**Windows:**

```bash
build_web_exe.bat
```

Или вручную:

```bash
python build_web_exe.py
```

**macOS/Linux:**

```bash
python3 build_web_exe.py
```

После сборки exe файл будет находиться в папке `dist/PDFEditorWeb.exe`

### Использование собранного exe

**Десктопное приложение:**

- Просто запустите `dist/PDFEditor.exe`
- Приложение откроется в графическом интерфейсе

**Веб-приложение:**

- Запустите `dist/PDFEditorWeb.exe`
- В консоли появится адрес (например, `http://0.0.0.0:5001`)
- Откройте этот адрес в браузере

### 🔄 Сборка Windows EXE на Mac

**Проблема:** PyInstaller не поддерживает кросс-компиляцию - нельзя собрать Windows exe на Mac напрямую.

**Решение:** Используйте GitHub Actions для автоматической сборки в облаке!

#### Вариант 1: GitHub Actions (Рекомендуется) ⭐

1. **Создайте GitHub репозиторий** (если еще нет):

   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ВАШ_USERNAME/pdf-editor.git
   git push -u origin main
   ```

2. **Запустите скрипт:**

   ```bash
   chmod +x build_windows_github.sh
   ./build_windows_github.sh
   ```

3. **Или вручную через GitHub:**
   - Откройте ваш репозиторий на GitHub
   - Перейдите в раздел **Actions**
   - Выберите workflow **"Build Windows EXE"**
   - Нажмите **"Run workflow"**
   - Дождитесь завершения (5-10 минут)
   - Скачайте exe файлы из раздела **Artifacts**

#### Вариант 2: Виртуальная машина Windows

1. Установите виртуальную машину (Parallels, VMware, VirtualBox)
2. Установите Windows в виртуальной машине
3. Установите Python и зависимости
4. Запустите `build_exe.bat` внутри Windows

#### Вариант 3: Облачный сервис

- Используйте GitHub Codespaces с Windows контейнером
- Или другие облачные сервисы с Windows (AWS, Azure)

### Примечания

- Размер exe файла может быть довольно большим (50-100 МБ), так как включает все зависимости
- Первый запуск exe может быть медленнее обычного запуска Python скрипта
- Для macOS и Linux PyInstaller создаст не exe, а соответствующие исполняемые файлы для этих платформ
- **GitHub Actions** - самый простой способ собрать Windows exe на Mac (бесплатно для публичных репозиториев)

## Лицензия

Этот проект предоставляется "как есть" для личного использования.

## Автор

PDF Editor - простой редактор PDF файлов с графическим интерфейсом.
